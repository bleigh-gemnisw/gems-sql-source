/*
  UB114 - Customer Meter/Deduct History
  Table: UTDEDDIFF

  09/01/2026 Ken - Initial creation for UB114.
*/

IF OBJECT_ID('dbo.UTDEDDIFF', 'U') IS NULL
BEGIN
    CREATE TABLE dbo.UTDEDDIFF
    (
        DDACCT   numeric(6,0) NOT NULL,
        DDMETER  CHAR(20) NOT NULL,
        DDDATE   numeric(8,0) NOT NULL,
        DDREAD   numeric(9,0) NOT NULL
            CONSTRAINT DF_UTDEDDIFF_DDREAD DEFAULT (0),
        DDDIFF   numeric(9,0) NOT NULL
            CONSTRAINT DF_UTDEDDIFF_DDDIFF DEFAULT (0),
        DDRESN   CHAR(3)  NOT NULL
            CONSTRAINT DF_UTDEDDIFF_DDRESN DEFAULT (''),

        CONSTRAINT PK_UTDEDDIFF
            PRIMARY KEY CLUSTERED (DDACCT, DDMETER, DDDATE)
    );

    PRINT 'SUCCESS: UTDEDDIFF table was created.';
END
ELSE
BEGIN
    PRINT 'NOTICE: UTDEDDIFF table already exists. No changes were made.';
END;
GO